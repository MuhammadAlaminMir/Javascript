//window is an Object
//window carry's everything
// In Browsers every tab re a Represent an individual window object
//every window object have location we can use window object to move another location
//the window object has meany method and Api
//when we run js in browser window is our global context
//every thing we do with js in js its adjust with window object
//we have to know about window object property , method , api etc . [So must research]
//when we make any kind of tab in browser its create a js instead
//Every thing we declare in html, js in browser it will be adjust in window

//In Dom window is the parent object
//and in js window is a global object who carry's every thing (var, func, arr, obj. etc)
