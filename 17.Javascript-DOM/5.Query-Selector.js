//Query Selector

// Query selector work like css selector like:\

let selectId = document.querySelector(
    "give # tag and then give the id name like css"
); //for select id

let selectClass = document.querySelectorAll(
    "give . and then give the class name like css "
); //for select all class

let selectTag = document.querySelectorAll("just give the tag name like css"); //for select all tagname

let selectAttribute = document.querySelectorAll(
    '[(give the attribute name)^="and give the value or name"]'
); //for select attribute

//when we using query that we got the element node
